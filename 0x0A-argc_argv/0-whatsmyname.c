#include <stdio.h>

/**
 * main - main function
 * argc - argument count
 * argv: argument vector
 * return: 0
 */

int main(int argc, char *argv[])
{
  argc = 0;

  printf("%s\n", argv[argc]);
  return (0);
}
